<resources xmlns:tools="http://schemas.android.com/tools">
    <!--
        Base XML theme used by the Activity window before Compose attaches.
        Compose owns the runtime theming via AlKhatibTheme.
    -->
    <style name="Theme.AlKhatib" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/al_khatib_screen_background</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@color/al_khatib_screen_background</item>
        <item name="android:windowBackground">@color/al_khatib_screen_background</item>
    </style>

    <style name="Theme.AlKhatib.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/al_khatib_screen_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_logo</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/al_khatib_screen_background</item>
        <item name="postSplashScreenTheme">@style/Theme.AlKhatib</item>
    </style>
</resources>
